name: firstenv
channels:
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - pytorch-cuda=11.8
  - torchaudio
  - torchvision
  - conda-forge::pytorch-lightning
  - conda-forge::wandb
  - conda-forge::scikit-learn
  - matplotlib
prefix: /home/ahmedlinux/miniconda3/envs/firstenv

# These were the first commands ran:
# conda create --name firstenv python=3.10
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia